# Synthetic energy-fleet model for the demo (all data is fictional):
#   config.py        — catalog/schema names + sizing knobs (env-overridable)
#   plants.py        — a fictional renewable portfolio (PLANTS) + type helpers
#   sensor_specs.py  — per-type sensor templates (alarm bands + generator hints)
#   fleet.py         — sensor enumeration + realistic value synthesis
# Imported by the setup seeder and the Zerobus producer (one source of truth).
